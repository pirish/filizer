apiVersion: v1
kind: Service
metadata:
  name: {{ include "filizer-server.fullname" . }}
  labels:
    {{- include "filizer-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "filizer-server.selectorLabels" . | nindent 4 }}
